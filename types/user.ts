export type User = {
    id: string;
    username: string;
    avatar_url: string;
    created_at: string;
    email: string;
    isDarkTheme: boolean;
};

export type ProfileType = {
    id: string;
    username: string;
    avatar_url: string;
    created_at: string;
};
